plugins {
    id 'application'
}

dependencies {
    implementation project(':common')
}

application {
    mainClass = 'chat.client.ClientMain'
}

// OBLIGATORIO: Permite que Gradle lea del teclado (System.in) en tareas interactivas
run {
    standardInput = System.in
}